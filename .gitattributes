# 语言检测规则（GitHub linguist）：
# 显式声明项目实际使用的每一类语言，确保 GitHub 语言栏如实统计与显示。
#
#   .java  → Java（主语言，业务逻辑 / Activity / 工具类）
#   .xml   → XML（Android 布局 / 资源 / Manifest）
#   .gradle→ Groovy（Gradle 构建脚本）
#   .md    → Markdown（README / CHANGELOG）
#   .yml / .yaml → YAML（GitHub Actions 工作流；scala.yml 实为 YAML 而非 Scala）
#   .pro   → ProGuard（混淆配置，属文本配置文件）
#
# 仅排除二进制安装包与压缩包（体积大且失真），避免干扰语言统计。

*.java linguist-detectable
*.xml  linguist-detectable
*.gradle linguist-detectable
*.md linguist-detectable
*.yml linguist-detectable
*.yaml linguist-detectable
*.pro linguist-detectable

*.apk linguist-vendored
*.zip linguist-vendored
_image/* linguist-vendored
_assets/* linguist-vendored
